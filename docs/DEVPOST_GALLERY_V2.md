# Free2PA Devpost Gallery v2

Goal: up to fifteen still images that match the simplified Hello World product
story and avoid the older Agentic Factory / Nerve Center framing.

Recommended size: 1800x1200 PNG.

## Upload order

1. `01-hero-load-gate.png`
   - Headline: Free2PA
   - Text: Tamper detection for agent control files before model context.
   - Visual: signed receipt, control file, LOAD gate.

2. `02-agent-control-files.png`
   - Shows `SOUL.md`, `AGENTS.md`, `SKILL.md`, `TOOLS.md`, prompts, policies,
     and manifests as files an agent loads.

3. `03-hello-world-agent.png`
   - The Hello World phone with only the green wave button ready to run.

4. `04-free2pa-verify-console.png`
   - The verify console phone before checks begin.

5. `05-check-signature.png`
   - Signature check visible and passing.

6. `06-check-file-hash.png`
   - File hash / receipt match check visible and passing.

7. `07-check-publisher-trust.png`
   - Trusted-publisher group check visible and passing.

8. `08-load-result.png`
   - Verification result `LOAD`; verified `SOUL.md` enters model context.

9. `09-model-reply.png`
   - Hello World app response: `Hello, <positive adjective> world!`

10. `10-edit-soul-md.png`
    - Editor changes one control word from `Never` to `Always`.

11. `11-signature-required.png`
    - Saved revision is pending; new signature required.

12. `12-changed-after-signature.png`
    - File hash fails; changed bytes rejected before context.

13. `13-restore-run-report.png`
    - Host falls back to last verified signed file and reports the rejected
      edit.

14. `14-sign-new-revision.png`
    - Local console signs the intentional edit and creates a new receipt.

15. `15-developer-install.png`
    - Install command, `loadVerifiedFile()` snippet, and surfaces:
      CLI / HTTP / MCP / CI / Codex skill.

## Notes

- Do not use `Nerve Center` in the image text.
- Do not use the old Agentic Factory layout.
- Use the Hello World demo as the main visual proof.
- Use `control files` and `load boundary` consistently.
- Include `SOUL.md`, `AGENTS.md`, `SKILL.md`, and `TOOLS.md` in at least one
  image for keyword clarity.
